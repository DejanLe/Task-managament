<%= form_for([@management, @management.tasks.build]) do |f| %>

  <%= f.text_field :name, class: 'form-control' , placeholder: 'Add Taks name for project' %>
 
  <%= f.text_area :description, class: 'form-control' , placeholder: 'Add Task description for projectt'  %>
  <br>
  <%= f.submit class: 'btn btn-outline-primary' %>
<% end %>
